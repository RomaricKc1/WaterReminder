package com.romarickc.reminder.presentation.utils

import com.romarickc.reminder.R

object Routes {
    const val RegIntake = (R.string.register_intake).toString()
    const val IntakeHistory = (R.string.intake_history).toString()
    const val HydrationTips = (R.string.hydration_tips).toString()
    const val NotifSettings = (R.string.notif_settings).toString()
    const val SetTarget = "set_target"
    const val Home = "home"
    const val SeeIntakeGraph = "seeintakegraph"
    const val ImportExport = "importexport"
    // I won't change anything, but can be set all in the string resource
}